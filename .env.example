# Copy to .env for local runs (never commit .env). On Hugging Face, set this in
# Space Settings > Variables and secrets instead.

# data.gov.in API key — get a free key at https://data.gov.in/
# Required to query the market-prices API (or paste it in the app sidebar).
DATA_GOV_API_KEY=
